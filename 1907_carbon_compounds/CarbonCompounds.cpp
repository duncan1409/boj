#include <iostream>
#include <vector>
using namespace std;
void permutation(string carbonEquation, vector<int> v)
{
	vector<vector<int>> v;
	string eq = carbonEquation;
	for (int i = 0; i < 3; i++)
	{
		if (eq[0] == '+' || eq[i] == '=')
		{
			
		}
	}
}
int main()
{
	return 0;
}