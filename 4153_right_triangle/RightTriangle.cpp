#include <iostream>
#include <cmath> //제곱
#include <algorithm> // 정렬
using namespace std;
void RightTriangle (int angles)
{
  int pita=0;
  for (int i=0; i<3; i++)
    pita=angles[i];
  if (sum ==)
}
int main(int args, const char* argv[])
{
  string angles;
  getline(cin, stdin);

  return 0;
}